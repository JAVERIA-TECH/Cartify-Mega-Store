import {Heart,ShoppingBag,Star,ArrowUpRight} from 'lucide-react'; import {Link} from 'react-router-dom';
export default function ProductCard({p,wish,setWish,add}){const liked=wish.includes(p.id);return <article className="product-card"><div className="product-image"><img src={p.image} alt={p.name}/><span className="product-badge">{p.badge}</span><button className={'heart '+(liked?'liked':'')} onClick={()=>setWish(p.id)}><Heart size={18} fill={liked?'currentColor':'none'}/></button></div><div className="product-info"><div className="rating"><Star size={14} fill="currentColor"/><b>{p.rating.toFixed(1)}</b><span>({p.reviews})</span></div><h3>{p.name}</h3><p>{p.description}</p><div className="price-row"><div><strong>PKR {p.price.toLocaleString()}</strong><del>PKR {p.oldPrice.toLocaleString()}</del></div><button className="add" onClick={()=>add(p)}><ShoppingBag size={17}/></button></div><Link className="view-link" to={`/product/${p.id}`}>View details <ArrowUpRight size={15}/></Link></div></article>}
