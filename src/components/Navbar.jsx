import {Link,NavLink,useNavigate} from 'react-router-dom'; import {Search,ShoppingBag,Heart,UserRound,Moon,Sun,Menu,X} from 'lucide-react'; import {useState} from 'react';
export default function Navbar({dark,setDark,cartCount,wishCount}){const [open,setOpen]=useState(false),[q,setQ]=useState('');const nav=useNavigate();const submit=e=>{e.preventDefault();if(q.trim())nav(`/shop?search=${encodeURIComponent(q.trim())}`)};return <header className="nav-wrap"><div className="nav"><Link className="brand" to="/"><img src="/assets/brand/cartify-logo-user.webp"/><span>Cartify</span></Link><nav className={open?'nav-links show':'nav-links'}>{[['/','Home'],['/shop','Shop'],['/shop/electronics','Electronics'],['/shop/fashion','Fashion'],['/shop/shoes','Shoes'],['/shop/jewelry','Jewelry'],['/shop/beauty','Beauty'],['/shop/perfumes','Perfumes'],['/about','About']].map(([to,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}>{label}</NavLink>)}</nav><form className="search" onSubmit={submit}><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search Cartify finds..."/></form><div className="nav-actions"><button className="icon-btn" onClick={()=>setDark(!dark)}>{dark?<Sun size={19}/>:<Moon size={19}/>}</button><Link className="icon-btn badge-icon" to="/wishlist"><Heart size={19}/>{wishCount>0&&<b>{wishCount}</b>}</Link><Link className="icon-btn badge-icon" to="/cart"><ShoppingBag size={19}/>{cartCount>0&&<b>{cartCount}</b>}</Link><Link className="icon-btn" to="/login"><UserRound size={19}/></Link><button className="mobile-toggle" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div></header>}
