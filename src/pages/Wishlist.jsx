import {Link} from 'react-router-dom'; import {Heart} from 'lucide-react'; import {products} from '../data/products'; import ProductCard from '../components/ProductCard';
export default function Wishlist({wish,setWish,add}){const list=products.filter(p=>wish.includes(p.id));return <main className="shop-page"><div className="shop-hero"><div><span className="eyebrow">SAVED FINDS</span><h1>Your wishlist</h1><p>Keep the Cartify products you love in one place.</p></div><div className="shop-count">{list.length}<span>saved</span></div></div>{!list.length?<div className="empty-card"><Heart size={40}/><h2>No saved finds yet</h2><p>Tap the heart on any product to build your wishlist.</p><Link className="btn primary" to="/shop">Browse finds</Link></div>:<div className="product-grid large">{list.map(p=><ProductCard key={p.id} p={p} wish={wish} setWish={setWish} add={add}/>)}</div>}</main>}
