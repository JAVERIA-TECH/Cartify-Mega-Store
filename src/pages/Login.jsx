import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {LockKeyhole,Mail} from 'lucide-react';
export default function Login({setUser}){const [email,setEmail]=useState('demo@cartify.store'),[pass,setPass]=useState('Cartify123!'),[mode,setMode]=useState('login');const nav=useNavigate();const submit=e=>{e.preventDefault();setUser({email,name:email.split('@')[0]});nav('/')};return <main className="auth"><div className="auth-card"><img src="/assets/brand/cartify-logo-user.webp"/><span className="eyebrow">CARTIFY ACCOUNT</span><h1>{mode==='login'?'Welcome back':'Create your account'}</h1><p>{mode==='login'?'Save your finds and continue shopping from where you left off.':'Create a local demo account for your Cartify experience.'}</p><form onSubmit={submit}><label><Mail/> Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label><LockKeyhole/> Password<input type="password" value={pass} onChange={e=>setPass(e.target.value)} required/></label><button className="btn primary full">{mode==='login'?'Sign in':'Create account'}</button></form><button className="switch" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Need an account? Create one':'Already have an account? Sign in'}</button><small>Local demo authentication is included for this Vercel-ready frontend. Connect a managed auth/database service before treating accounts as production-secure.</small></div></main>}
